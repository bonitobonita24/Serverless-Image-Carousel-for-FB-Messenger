# Cloudflare Pages configuration
# Docs: https://developers.cloudflare.com/pages/configuration/wrangler-configuration/

name = "static-image-gallery"
compatibility_date = "2025-01-01"
main = "index.js"

# Pages project - static assets served from the `public` directory
[site]
bucket = "./public"

# Custom headers for optimal caching and Messenger compatibility
# (These are also defined in public/_headers for Pages)
